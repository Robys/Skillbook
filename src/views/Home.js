import {Button} from 'react-bootstrap'

const Home = () =>{

    return (<div>
        <h1>Bem vindo ao SkillBook</h1>

        <Button variant="primary" href="/access">Começar</Button>
    </div>)
}

export default Home